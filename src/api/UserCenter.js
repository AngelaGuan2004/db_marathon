/*
export function completePlayerInfor(params) {
    return request({
      url: '/Auth/update_player',
      method: 'post',
      data:{
        ID:params.ID,
        name:params.name,
        gender:params.gender,
        age:params.age,
        Id_number:params.Id_number,
        region:params.region,
        Telephone_number:params.Telephone_number,
        passward:params.passward,
      }
    })
}

export function completeVolunteerInfor(params) {
    return request({
      url: '/Auth/update_volunteer',
      method: 'post',
      data:{
        ID:params.ID,
        name:params.name,
        Telephone_number:params.Telephone_number,
        passward:params.passward,
      }
    })
}


export function getPlayerInfor(ID) {
    return request({
        url: '/Auth/xxx',
        method: 'get',
        params: { ID }
    })
}

export function getPhotoInfor(ID) {
    return request({
        url: '/Auth/xxx',
        method: 'get',
        params: { ID }
    })
}


export function getVolunteerInfor(ID) {
    return request({
        url: '/Auth/xxx',
        method: 'get',
        params: { ID }
    })
}


export function getMyRegistrations(ID) {
    return request({
        url: '/Player/xxx',
        method: 'get',
        params: { ID }
    })
}

export function getMyVolunteering(ID) {
    return request({
        url: '/Volunteer/xxx',
        method: 'get',
        params: { ID }
    })
}

*/