/*import request from '@/utils/request';

export function getParticipantInfo(eventId) {
  return request({
    url: `/api/event/${eventId}/participant`,
    method: 'get'
  });
}

export function registerParticipant(eventId, participant) {
  return request({
    url: `/api/event/${eventId}/register`,
    method: 'post',
    data: participant
  });
}*/
